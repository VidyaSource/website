---
import {Image} from 'astro:assets';
import { images} from "../../components/Images";
interface PostImageProps {
    width: number
    height: number
    alt: string
    src: string
}

const {height, width, alt, src} = Astro.props
console.log(src)

---
<div class="relative mx-auto m-5">
    <Image alt={alt} src={images[`/src${src}`]()} class="w-full h-auto object-cover" />
</div>